OpenGL viewer fix notes
=======================

- Added GLSL precision qualifiers so the custom ray shader compiles on OpenGL ES / ANGLE contexts.
- After reinstall, run `python examples/ellipse_interactive.py` and the rays should render; press `v` to toggle hit markers.
- If you need a static check, capture a screenshot once the rays appear and drop it here for reference.
Shader fix attempt 2 (no leading newline).
Shader fix attempt 3 (explicit join + ASCII comments).
Added modifier-aware sliders, overlay entry, and marker toggling fixes.
Clamped ray width in screen space to eliminate sub-pixel aliasing; defaults tuned for 1e-3 widths.
